# Shared Docker Compose defaults for local development only.
# Do not use these credentials in deployed environments.
POSTGRES_DB=saas_template
POSTGRES_USER=saas_template
POSTGRES_PASSWORD=local_development_only
POSTGRES_PORT=5432
REDIS_PORT=6379
