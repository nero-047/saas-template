# Local development defaults only. Do not reuse these credentials in deployed environments.
POSTGRES_DB=saas_template
POSTGRES_USER=saas_template
POSTGRES_PASSWORD=local_development_only
POSTGRES_PORT=5432
REDIS_PORT=6379

DATABASE_URL=postgresql://saas_template:local_development_only@127.0.0.1:5432/saas_template
REDIS_URL=redis://127.0.0.1:6379
