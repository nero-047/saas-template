# Admin application

`admin` is the future operator-facing application. It will consume generated
TypeScript API contracts for explicitly authorized platform administration and
support workflows.

It is not a tenant customer UI, and the identity foundation does not add login
screens or operator features.
