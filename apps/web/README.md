# Web application

`web` is the future customer-facing SaaS application. It will consume generated
TypeScript API contracts for authenticated user, organization, workspace, and
permission-aware product experiences.

No authentication pages or product UI are included in the identity foundation.
